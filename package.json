{
  "name": "bread-cloudflare-probe",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare-native server monitoring panel and lightweight probe agent.",
  "type": "module",
  "scripts": {
    "cf:login": "wrangler login",
    "setup": "node scripts/setup.mjs",
    "deploy": "wrangler deploy",
    "update": "wrangler deploy",
    "init-db": "node scripts/init-db.mjs",
    "check": "node --check worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
